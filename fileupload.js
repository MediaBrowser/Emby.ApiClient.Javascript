define([], function () {
    'use strict';

    function FileUpload() {

    }

    FileUpload.prototype.upload = function (file, name, url) {

        return Promise.reject();
    };

    return FileUpload;
});